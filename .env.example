# MediaVision Enrichment Pipeline — Environment Config
# Copy this file to .env and fill in your values

# Server port (default: 3000)
PORT=3000

# All API keys are configured inside the app Settings panel
# and stored securely in the local SQLite database.
# No keys need to be set here unless you want to pre-seed them.
